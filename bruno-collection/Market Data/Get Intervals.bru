meta {
  name: Get Intervals
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/intervals
  body: none
  auth: none
}

docs {
  # Get Available Intervals
  
  Returns all available time intervals for candle data.
  
  ## Response
  Array of interval strings (e.g., "1", "5", "15", "30", "60", "D")
}
